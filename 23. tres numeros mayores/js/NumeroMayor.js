/*
 * Numero Mayor
 * Autor: Diego De Jesus Arias Gonzalez
 * Fecha 4 de abril de 2024
 * version 1.0
 */


function numeroMayorParametro(numUno, numDos, numTres){
        
    if(numUno > numDos && numUno > numTres) {
    console.log("El numero uno es mayor: " +numUno);

    }else{
     if(numDos > numUno && numDos > numTres){;
     console.log("el numero dos es el mayor: " +numDos);

    }else{
        console.log("el numero tres es mayor: " +numTres);
    }
}
return "Final del numero"
}


const numeroMayorExpresion = function(numUno, numDos, numTres){
        
    if(numUno > numDos && numUno > numTres) {
    console.log("El numero uno es mayor: " +numUno);

    }else{
     if(numDos > numUno && numDos > numTres){;
     console.log("el numero dos es el mayor: " +numDos);

    }else{
        console.log("el numero tres es mayor: " +numTres);
    }
}
return "Final del numero"
}